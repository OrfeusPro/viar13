# SA E2E Smoke (2026-03-02)

## Scope
- Step 2: end-to-end message flow (inbound/outbound/status + chat binding + attachments).
- Step 3: real order flow on a fresh order created via `Orders::saveOrder`.

## Runtime
- Base URL: `https://viarcanvas.loc/api`
- Auth: `X-Api-Key: test-key`

## Step 2 Results
1. Message flow to existing real order `16686`:
- `POST /api/sa/webhooks/messages` (`message.created`) -> `status=ok`
- `POST /api/crm/webhooks/send-message` -> `status=ok`
- `POST /api/sa/webhooks/messages` (`message.status`) -> `status=ok`, `result.status_updated=true`

2. DB checks:
- `order_user_comments` contains new inbound and outbound comments for order `16686`.
- `orders_chats` remains untouched (expected behavior for client chat path).
- `sa_messages` contains inbound/outbound rows for `orders_id=16686`.
- `orders.sa_conversation_id` and `orders.sa_client_phone` updated.

3. Attachments:
- External URL attachment (`https://via.placeholder.com/...`) was rejected with `download_failed`.
- Data URL attachment (`data:image/png;base64,...`) was stored successfully.
- Stored attachment link is rendered into message text/comment using `APP_URL` base:
  - example: `https://viarcanvas.loc/storage/sa/attachments/...`

## Step 3 Results
1. New order creation via `Orders::saveOrder`:
- Created order id: `126977`
- Note observed: PHP Notice `Undefined index: terms_price` in `Orders.php:623` (order still created).

2. API smoke on `lead_id=126977`:
- `POST /api/sa/webhooks/messages` (`message.created`) -> `status=ok`
- `POST /api/crm/webhooks/pipeline-changed` (`STG-10 -> STG-20`) -> `status=ok`, `order_updated=true`
- `POST /api/sa/escalations`:
  - first attempt failed (`VALIDATION_ERROR`) due wrong payload shape (`data` instead of `escalation`).
  - corrected payload (per API contract/tests) -> `status=ok`, task created.

3. DB checks for `126977`:
- `orders.status` changed to `pegging`.
- `orders.sa_conversation_id` and `orders.sa_client_phone` populated.
- `sa_messages` contains inbound rows for the order.
- `order_user_comments` contains inbound comments for the order.
- `sa_escalations` contains a new row with `task_ref`.
- `orders.sa_bot_mode` updated to `handoff_to_manager` after escalation.

## Conclusion
- Steps 2 and 3 are functionally confirmed on real DB data and real `orders.id` binding.
- Remaining technical follow-up:
  - investigate `terms_price` notice in `Orders::saveOrder` path,
  - improve attachment external-download reliability (network/SSL handling).
