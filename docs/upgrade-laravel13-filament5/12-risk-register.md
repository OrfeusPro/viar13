# 12. Реестр рисков

Шкала: вероятность P и влияние I — Low/Medium/High/Critical.

| ID | Риск | P / I | Признак | Предотвращение | Восстановление | Этап |
|---|---|---|---|---|---|---|
| R-01 | неверный PHP binary даёт ложный baseline | High / High | разные `php -v`/ini | явные paths, CI matrix | повторить диагностику | AUD/PHP |
| R-02 | потеря/изменение `orders.id` или статусов | Low / Critical | count/hash/status diff | same schema, invariant tests | stop, restore DB/app | DB/APP |
| R-03 | duplicate/lost payment | Medium / Critical | charge vs DB mismatch | signed callback + receipt idempotency + reconciliation | disable endpoint, manual reconcile | INT |
| R-04 | SA webhook replay/race | High / Critical | duplicate message/status | unique claim + transaction + outbox | replay durable events | INT |
| R-05 | потеря переводов/SEO URL | Medium / Critical | locale count/404/rank drop | preserve 64 779 rows, route/301 manifest | restore translations/routes | APP/FIL |
| R-06 | permission escalation в Filament | Medium / Critical | role sees forbidden action | deny-by-default policies, 8-role matrix | panel off, Voyager fallback | FIL |
| R-07 | неполный перенос 133 BREAD/custom UI | High / High | missing action/filter/field | module manifest + UAT per Resource | re-enable Voyager module | FIL |
| R-08 | SwiftMailer removal ломает SMTP | High / High | boot/send failure | isolate purpose, Symfony Mailer tests | legacy mail release / queue pause | L9/INT |
| R-09 | Flysystem 3 меняет file semantics/root | High / Critical | missing/overwritten attachments | explicit disks, file corpus/hash | restore file snapshot | L9/DB |
| R-10 | Venipak label duplicated/credentials leaked | Medium / High | duplicate labels/log secret | idempotency, secret store, redaction | revoke/rotate, void label | INT |
| R-11 | PayPal SDK несовместим/устарел | High / Critical | cannot install/capture | official adapter spike + fixtures | keep legacy isolated service | INT |
| R-12 | route precedence/duplicates меняют endpoint | High / High | route snapshot diff | resolve missing controller, unique names, cached/uncached compare | old route artifact | L7/L12/L13 |
| R-13 | пересборка/оптимизация вручную поддерживаемых public assets меняет CSS или порядок jQuery plugins | High / High | production: Mix config/manifest есть, Vite отсутствует; manifest+lock 2022, package.json 2026 | не мигрировать на Vite; byte-identical frozen manifest/hash corpus; visual E2E | вернуть точный legacy asset snapshot | FE |
| R-14 | Carbon/date/timezone меняет API/scheduler | Medium / High | JSON/times/jobs shifted | legacy serializeDate, controlled clock | target config/old release | L7/L12 |
| R-15 | local empty SA tables скрывают production cases | High / High | unseen payload/data shape | anonymized production stats/fixtures | adapter feature off | AUD/INT |
| R-16 | мало FK, orphan data ломает Resources | High / High | relation null/errors | orphan audit, nullable-safe UI | quarantine records/forward fix | DB/FIL |
| R-17 | DB migration lock/disk outage | Medium / Critical | lock queue/disk spike | EXPLAIN/online DDL/window/headroom | stop DDL, fail back | DB/DEP |
| R-18 | queue production на `sync`/без worker policy | High / High | timeouts/backlog | discover production, supervisor/failed jobs | switch sync selectively, pause producers | DEP/INT |
| R-19 | OAuth account takeover/link collision | Medium / Critical | duplicate email/provider ID | verified linking rules, unique constraints | disable provider, unlink/review | INT |
| R-20 | upload XSS/malware/IDOR | High / Critical | executable/public file | MIME decode/size/policy/private storage | quarantine/delete/incident | APP/FIL |
| R-21 | secrets/default `test-key` попадут в prod | Medium / Critical | known key accepted | no defaults, secret scan/rotation | rotate/revoke/audit logs | DEP |
| R-22 | callback blocked by Laravel 13 origin/CSRF | Medium / High | provider 419/403 | explicit route middleware contract tests | legacy endpoint/exception fix | L13/INT |
| R-23 | production topology неизвестна | High / Critical | cutover steps unverifiable | AUD-01 + restore rehearsal | postpone release | AUD/DEP |
| R-24 | giant controllers cause hidden behavior loss | High / High | target diff after refactor | characterization + strangler adapters | route to legacy handler | APP |
| R-25 | parallel Voyager/Filament lost update | Medium / High | conflicting timestamps/content | one write-owner per module | freeze one UI, restore row | FIL |
| R-26 | заказы продолжают поступать, а staging snapshot устаревает | High / Critical | target не видит новые/изменённые rows | legacy system of record, регулярные snapshots, watermark/delta reconciliation | не переключать; повторить sync/reconciliation | DB/DEP |
| R-27 | rollback старого dump удалит заказы, созданные после backup | Medium / Critical | DB `MAX(id)`/payment count уменьшается | application rollback отдельно от DB; PITR и immutable binlog | остановить writes, PITR + provider reconciliation | DEP/RBK |
| R-28 | callback/webhook попадает между старым и новым релизом | Medium / Critical | provider retry/unknown event/duplicate | atomic route ownership, общий dedupe store, buffer/replay | вернуть legacy endpoint, replay durable events | INT/DEP |
| R-29 | редакторы меняют переводы после снятия staging snapshot | High / Critical | target показывает старый текст/slug/SEO | общий storage или incremental sync; per-module write-owner; manifest diff | оставить Voyager write-owner, повторить delta sync | DB/FIL/DEP |
| R-30 | Voyager и Filament одновременно сохраняют один перевод | Medium / High | lost update, разные `updated_at`/hash | read-only второй UI, optimistic lock/version audit | вернуть значение из audit/backup, отключить второй write | FIL |
| R-31 | чувствительный `/admin` endpoint не имеет фактической auth/role защиты | High / Critical | static L2: у 38 из 58 routes auth enforcement не обнаружен | route-action-role matrix, middleware/policies deny-by-default, staging negative tests | закрыть route/WAF rule, вернуть legacy protected handler, audit access logs | AUD/FIL/SEC |
| R-32 | создание заказа частично завершается между DB write, mail, Synvolve, coupon/bonus и session | Medium / Critical | order есть, а side effect отсутствует/повторён | characterization, DB transaction для local state, durable outbox, checkout idempotency | reconcile order/coupon/bonus, replay outbox без повторного order insert | APP/INT |
| R-33 | перенесены DB-переводы, но потеряны PHP lang files или API writers | High / High | UI keys/Blog/SA отличаются после cutover | inventory двух storage-контуров и всех writers, per-module owner, insert-update-delete reconciliation | вернуть writer legacy, повторить file+DB delta sync | APP/FIL/DEP |
| R-34 | GET/`Route::any` запускает изменение данных, письмо, cache command или file write | High / Critical | подтверждено 15 GET-capable side-effect routes | POST/PATCH/DELETE + CSRF + policy; запрет prefetch; characterization tests | временно закрыть route/WAF, отключить side effect, сверить audit/mail/provider logs | APP/SEC |
| R-35 | одиночная очистка `../` в public file route допускает повторное появление traversal | Medium / Critical | `....//secret.svg` после текущего `str_replace` становится `../secret.svg` | canonical `realpath`, root containment, private storage+ownership, encoded-path suite | закрыть file routes, перевести отдачу через контролируемый download, audit access logs | APP/SEC |
| R-36 | product families и каналы создания заказа рассчитывают цену по разным multiplier/count/rounding правилам | High / Critical | static L2: double multiplier в modular/generic component branch, canvas count omitted, portrait `(int)`; Admin/SA имеют отдельные calculators | golden money corpus, утверждённый authority manifest, compatibility adapter, differential public/Admin/SA tests | оставить legacy price owner, остановить target writes, reconcile `price/sale_price/items/payment` | APP/FIN |
| R-37 | coupon/bonus boundary и type semantics дают другой или отрицательный итог | Medium / Critical | friend subtotal без floor; date thresholds; unit-price promotions игнорируют count; `sale_price` string | boundary fixtures, Money value object на target boundary, бизнес-утверждение каждого coupon type | отключить конкретный coupon type, сохранить legacy calculator, reconcile user/coupon/order_action | APP/FIN |
| R-38 | SA minimal fallback создаёт настоящий `orders.id` с пустыми items/нулевой ценой и может считаться полноценным заказом | Medium / High | `resolveOrCreateLeadId()` fallback сохраняет order после mapping/save failure | явный source/completeness flag, UAT и downstream filters, atomic linking/replay | временно отключить auto-create service, маркировать и вручную дополнить order | CRM/APP |
| R-39 | payment signing/API credential хранится в tracked PHP code или отсутствует в typed config | High / Critical | Paysera signing password — controller constant; PayPal env contract отсутствует в `.env.example` | немедленная ротация, secret manager, typed config, secret scan, sandbox/live gate | revoke/rotate, проверить provider/access logs, остановить create/callback при сомнении | PAY/SEC |
| R-40 | ручной payment-status endpoint доступен без auth/role и повторяет финансовые side effects | High / Critical | public POST `/orders/change/payment`; `payed→payed` повторяет bonus/gift-card/PDF/mail/Synvolve | deny-by-default policy, enum transition, atomic receipt claim, outbox, replay tests | закрыть route/WAF, заморозить manual status, reconcile coupons/bonuses/mail/provider | PAY/SEC |
| R-41 | signed/captured provider response помечает оплату без expected amount/currency/reference/unique receipt | High / Critical | Paysera helper не вызывается; PayPal capture body игнорируется; receipt table отсутствует | immutable attempts/receipts, exact verification, unique provider transaction, callback authority | остановить auto-fulfilment, provider↔DB reconciliation, исправить статус forward-only | PAY/FIN |
| R-42 | browser accept/cancel/thanks и provider callback имеют пересекающиеся side effects | High / High | Paysera cancel может ставить paid; accept+callback пишут; thanks GET повторяет mail | callback sole authority, read-only return pages, unique outbox event | отключить mail на GET, dedupe notifications, replay только подтверждённый receipt | PAY/APP |
| R-43 | payment request не имеет expiry/attempt history/collected balance/refund states | Medium / High | только pending/paid; несколько requests на order; provider ID перезаписывается | payment ledger, expiry/state machine, balance/overpayment rule, reconciliation | отключить старый link, ручная сверка доплат, не менять parent order автоматически | PAY/FIN |
| R-44 | счета и private order/integration files доступны по предсказуемому public URL | High / Critical | invoice path = `public/storage/pdf/{orders.id}.pdf`; order/SA files лежат на public disks | private storage, ownership Policy, controlled download/signed URL, access audit | закрыть public route/CDN path, rotate URLs, проверить access logs и уведомить incident owner | FILE/SEC |
| R-45 | file/image endpoints позволяют чужую или неавторизованную запись/удаление | High / Critical | public order file actions; custom admin routes вне `admin.user`; account upload без order/painter ownership | deny-by-default route+Policy, own/assigned-order checks, negative 8-role matrix | закрыть routes/WAF, заморозить uploads, сверить order/image rows и mail logs | FILE/SEC |
| R-46 | file move/delete и DB reference switch частично завершаются | High / Critical | `renameUploadsPhoto()` удаляет source до финального order save; PDF overwrite без atomic publish | copy+hash+decode, DB switch transaction, after-commit cleanup, artifact journal/failpoints | восстановить source/target по hash, forward-fix references, не откатывать старый DB dump | FILE/DB |
| R-47 | base64/raw SVG/аудио и разрозненные uploads обходят единый size/MIME/decode policy | High / Critical | non-strict decode, raw public SVG, `max:9000000`, TinyMCE original-name overwrite | central upload intent/policy, real MIME/decode, aggregate limits, quarantine, random names | отключить тип/endpoint, quarantine corpus, удалить только после reference/backup audit | FILE/SEC |
| R-48 | PDF renderer одновременно меняет DB, зависит от двух контуров переводов и публикует недетерминированный artifact | Medium / Critical | missing user создаётся во время render; arbitrary `pdf_locale`; direct public overwrite; gift-card route arity mismatch | pure renderer, locale/template snapshot, artifact receipt, temp+atomic publish, seven-locale golden corpus | вернуть прежний artifact по receipt/hash, отключить regeneration, исправить статус forward-only | PDF/APP |
| R-49 | SA attachment downloader допускает SSRF/MITM и memory DoS | Medium / Critical | arbitrary URL + redirects; TLS verify off; whole body read before size check; public result | HTTPS/host egress allowlist, DNS/IP validation each redirect, TLS on, streamed byte cap, private storage | отключить attachments, rotate API key, audit egress/access logs, quarantine downloaded files | CRM/SEC |
| R-50 | browser управляет delivery method/price/point, а order/payment доверяют session snapshot | High / Critical | `setdelivery()` принимает `price`, country, method и pickup fields без server quote/lookup; значение становится `orders.delivery.deliv_price` | server-authoritative quote token, country/method/coupon registry, point/workshop existence checks, recompute before payment | оставить legacy checkout owner, отключить проблемный method, reconcile delivery price/provider/payment | DEL/FIN |
| R-51 | Venipak directory outage/abuse блокирует checkout | High / High | до двух provider calls на page; публичные credentialed lookup routes; нет timeout/cache/status/cURL error handling | typed client, short timeout, throttle, cache+freshness, stale-last-good fallback, circuit/metrics | временно скрыть pickup method, использовать утверждённый cache, не ставить нулевую цену | DEL/INT |
| R-52 | невалидные/неэкранированные поля изменяют или ломают shipment XML | Medium / Critical | XML строится конкатенацией raw fields; address validation пропускает partial; package arrays/ranges не проверены | FormRequest/DTO, XMLWriter/DOM, schema/golden XML, postcode/phone/package limits | остановить auto-submit, исправить draft и создать новую controlled attempt | DEL/INT |
| R-53 | label/courier side effect повторяется или остаётся с неизвестным outcome | High / Critical | нет attempt ledger/idempotency/receipt/void; provider accept может произойти до сбоя DB; `orders.labels` перезаписывается | durable unique attempt claim, request/response hash, state machine, reconciliation, void/cancel, no blind retry | запретить повтор unknown attempt, сверить provider, forward-fix receipt/order projection | DEL/INT |
| R-54 | Venipak secrets/URLs скомпрометированы или направляют запрос на неверный host | High / Critical | plaintext DB/mass assignment; tracked historical/default credentials; DB-editable URLs без allowlist | немедленная ротация, secret manager, no defaults, environment/live gate, HTTPS host allowlist | revoke/rotate, блок outbound, audit DB/backups/provider access | DEL/SEC |
| R-55 | label PDF/point identity не связаны с разрешённым order | High / Critical | print route без auth/ownership; arbitrary code; любой non-Error body объявляется PDF; public pickup ID не сохраняется | Policy+role/order link, provider point immutable snapshot, PDF status/type/signature/size/hash, safe filename/audit | закрыть print/lookup route, проверить provider/access logs, reprint только reconciled labels | DEL/SEC |
| R-56 | публичный поиск пользователя раскрывает credential/PII columns | High / Critical | `POST /admin/check-user` имеет только `web` и возвращает raw DB rows, включая hash/remember token | немедленно закрыть route, вернуть allowlisted DTO, rate limit и audit | WAF/route off, проверить access logs, сбросить затронутые sessions/tokens | AUTH/SEC |
| R-57 | authenticated IDOR позволяет менять чужой заказ, чат, изображения и read flags | High / Critical | account mutation routes доверяют `order_id`, `chatId` и image IDs без ownership/role scope | Policies, scoped binding, actor/order invariant и отрицательная role×ownership matrix | отключить mutation group, audit rows/files/mail по actor/order | AUTH/APP |
| R-58 | обычный пользователь может выдать сообщение за admin и запустить внешние side effects | High / Critical | `send_admin_to_client_painter_comments` под одним `auth`, записывает `is_admin=1`, шлёт mail/Synvolve | отдельная staff permission, server-side author type, outbox и audit | route off, уведомить владельца, reconcile chat/mail/Synvolve | AUTH/CRM |
| R-59 | параллельные auth contracts обходят CAPTCHA/парольную политику и оставляют plaintext password path | High / Critical | standard и custom login/register активны одновременно; mail template получает исходный пароль | единый auth contract, throttle/CAPTCHA policy, regenerate session, никогда не передавать пароль в mail | выключить custom/standard bypass, forced reset и mail/log audit | AUTH/SEC |
| R-60 | OAuth state/linking/redirect правила допускают подмену или неверное связывание аккаунта | Medium / Critical | `stateless()`, email-only `firstOrCreate`, provider subject не хранится, prefix redirect check | stateful OAuth, verified-email/link confirmation, unique provider subject, exact-origin allowlist | provider off, unlink/review collisions, revoke tokens/sessions | AUTH/INT |
| R-61 | четыре legacy chat streams имеют неоднозначного автора, orphans и слабые индексы | High / High | `order_user_comments`, `orders_chats`, `order_admin_comments`, `order_painter_comments`; часть rows без author/order | additive actor/source fields, nullable-safe mapping, order/time indexes после EXPLAIN, retention decision | legacy reader/write-owner, quarantine only after owner approval | CHAT/DB |
| R-62 | повтор chat request дублирует row, mail и webhook; partial outcome не восстанавливается | High / High | нет client message ID/idempotency/outbox; порядок DB/mail/Synvolve неодинаков | unique message claim, transaction local state, after-commit outbox, reconciliation | остановить retry, dedupe/reconcile по actor/order/time/hash | CHAT/INT |
| R-63 | chat text попадает в DOM/HTML mail/log без единой escaping/redaction policy | High / Critical | AJAX template literal `.append()`, `text/html` mail body и full Synvolve payload logs | text-only contract, contextual escaping/sanitization, CSP, redaction tests | закрыть affected UI/mail path, purge unsafe logs/content, incident review | CHAT/SEC |
| R-64 | state-changing GET и unsigned unsubscribe доступны без достаточной авторизации | High / Critical | `new/set_all_painter_images` меняет все orders и вызывает `dd`; unsubscribe меняет user по numeric ID | удалить/закрыть utility, POST+CSRF+policy; signed unsubscribe URL | WAF/route off, audit writes, восстановить affected flags | AUTH/SEC |
| R-65 | локальные session/OAuth/runtime настройки не доказывают production readiness | High / High | local `APP_DEBUG=true`, secure cookie unset, callbacks на другой host, stale sessions/resets | environment parity checklist, secrets/redirect console audit, secure/SameSite policy, cleanup jobs | postpone cutover, return legacy auth endpoints | DEP/AUTH |
| R-66 | public mail preview GET меняет tokens/coupons или раскрывает private user/order data | High / Critical | `/mail/abandoned_cart*`, `/mail/order`, `/mail/gift_cart`, `/mail/test` доступны через `web` | закрыть production routes; local/admin synthetic fixtures; GET без DB diff | WAF/route off, audit tokens/coupons/access logs, invalidate affected links | MAIL/SEC |
| R-67 | SMTP TLS verification отключена, а SwiftMailer customization блокирует Laravel 9+ | High / Critical | peer/name verify false, self-signed true; `getSwiftMailer()` в boot | включить TLS verify/CA/host policy; подтвердить EHLO contract; Symfony Mailer adapter | остановить outbound, rotate credentials при MITM suspicion, вернуть проверенный mail release | MAIL/SEC |
| R-68 | synchronous mail внутри order/payment/chat request создаёт partial state и duplicate при retry | High / Critical | queue default sync; direct SMTP до/после DB/file/provider writes | domain event + after-commit outbox + deterministic message key | оставить legacy business state, reconcile, replay только unsent receipt | MAIL/APP |
| R-69 | queue/worker/failed/restart topology и provider delivery outcome не доказаны | High / High | jobs/failed_jobs локально пусты; worker manifests/receipts отсутствуют | production inventory, managed purpose queues, provider IDs, metrics/runbook | pause producers, drain/reconcile known attempts, controlled retry | QUEUE/DEP |
| R-70 | file-cache scheduler locks не обеспечивают one-server/heartbeat contract | High / High | five events используют default 1440-min mutex; cache=file; cron hosts неизвестны | distributed lock, `onOneServer`, explicit TTL/timezone, run ledger/alert | disable duplicate cron host, clear lock by runbook, rerun only reconciled batch | CRON/DEP |
| R-71 | abandoned-cart cleanup/token/coupon flags расходятся с enqueue/delivery | High / High | cleanup before query; token before queue; coupon before try; expired tokens; no attempt ledger | campaign state machine, unique claim, outbox/receipt, terminal-state retention | stop campaign, preserve rows, reconcile coupons/tokens/provider attempts | MAIL/MKT |
| R-72 | marketing consent/unsubscribe/suppression contract нарушен | High / Critical | `news=NO` ignored; unsubscribe `#`/external placeholder/unsigned ID; no bounce/complaint | template registry, consent ledger, signed one-click suppression, provider webhooks | halt marketing, suppress affected recipients, compliance review | MAIL/LEGAL |
| R-73 | overdue first run создаёт batch, send-before-marker допускает duplicate | Medium / High | local read-only: 59 due, 0 marked; marker saved after SMTP; no claim | dry-run/max batch, atomic claim/outbox, provider receipt, concurrency test | pause command, reconcile sent/marker/provider, resume by unsent IDs | MAIL/OPS |
| R-74 | global locale/random content/remote assets делают email nondeterministic | High / High | 19 `App::setLocale`; random top content; 785 remote URL refs | immutable template/version/locale/content snapshot, restore locale, asset manifest | freeze template, resend only approved snapshot, fallback assets | MAIL/CONTENT |
| R-75 | raw HTML и recipient/subject logging создают injection/privacy risk | High / Critical | 25 `setBody(text/html)`, raw Blade echo, single PII `mail.log` | typed escaped templates/sanitizer, redacted telemetry, rotation/retention | disable unsafe template, quarantine logs/content, incident review | MAIL/SEC |
| R-76 | queued PDF path не immutable и MIME неверен | Medium / High | three attachments use `text/pdf`; async worker may see missing/replaced path | private immutable artifact receipt/hash, `application/pdf`, size/type validation | suppress attempt, regenerate verified artifact, controlled resend | MAIL/FILE |
| R-77 | alt/media jobs не используют ожидаемые retry/atomic semantics | High / High | catch-all marks success; sync observer blocks request; image file/DB switch non-atomic | exception taxonomy/rethrow, async after-commit queues, artifact receipt/checkpoint | disable observer/schedule, reconcile suggestion/files, retry selected IDs | QUEUE/MEDIA |
| R-78 | public catalogue maintenance GET меняет DB без auth/transaction | High / Critical | `/set_sizes|set_genre|set_style` создают/меняют pivots, `dd` и signature mismatch | закрыть public routes; authorized CLI/admin dry-run, batch, transaction, receipt | route/WAF off, audit writes, restore from reconciled IDs only | CAT/SEC |
| R-79 | новая gallery item route возвращает 404 и type/category scope неоднозначен | High / Critical | `getBy(false)` при всех непустых category URLs; category не scoped by type | primary-category/canonical contract, separate hotfix, route fixtures | return legacy item route, preserve IDs/URLs | CAT/SEO |
| R-80 | route registry неполон и duplicate names зависят от порядка | High / High | missing `ImageController`; GET/POST ram search share name | restore route boot, unique names, route snapshot/cache tests | disable unresolved route, legacy registry | APP |
| R-81 | языки/переводы теряются из-за `ee/et/uk` и разных fallback stores | High / Critical | public 7 locales; DB `et`; SA requires `uk`; PHP+Voyager+base fields | preserve all codes/rows/files, locale count/hash/delta matrix | legacy translation writer, restore missing rows from journal | I18N |
| R-82 | display/cart/order/SA pricing расходится | High / Critical | custom strings, options, country/IP multiplier, session discount и API mapping | one server pricing authority + versioned option IDs; cross-channel fixtures | keep legacy calculator owner, reject unmatched version | MONEY/CAT |
| R-83 | generator/client upload и price payload становятся unsafe/reachable | High / High | dead Simpsons/Sharj handlers trust client price/base64/files | reachability registry, strict upload policy, server recompute, negative tests | route off, purge quarantined files, discard basket line | CAT/FILE |
| R-84 | orphan pivots/отсутствующие indexes теряют данные и замедляют filters | High / High | 28/37/31/66 item orphans; pivots mainly primary-only | ID quarantine, owner decision, EXPLAIN, additive online indexes | retain orphans, rollback index, legacy queries | DB/CAT |
| R-85 | N+1/random/unbounded catalogue queries создают latency/nondeterminism | High / High | per-ID queries, translation LIKE, `inRandomOrder`, repeated global sets | production profile, deterministic fixtures, semantic diff, bounded queries | revert query slice, disable expensive filter by flag | PERF/CAT |
| R-86 | пересборка/порядок manual public assets ломает generators/UI | High / Critical | production использует legacy Mix artifact; package/lock/manifest drift, Vite artifact отсутствует; 12 outputs/load order значимы | byte-identical copy, manifest+SHA/load-order/browser gate; no Vite/rebuild by default | restore exact production artifact/CDN version | FE |
| R-87 | hardcoded production host/CDN/fonts создают CSP/availability coupling | Medium / High | viarcanvas.com, jsDelivr, cdnjs, Google Fonts in Blade/JS | inventory/pin/SRI/self-host decision and outage fixtures | retain approved URL/fallback asset | FE/SEC |
| R-88 | raw translated/Voyager/user HTML ломает context или допускает XSS | High / Critical | many `{!! !!}` in body/attribute/inline JS; review text raw | field/context registry, allowlist sanitizer, contextual encoding tests | disable field/template, restore sanitized version | SEC/CONTENT |
| R-89 | GET item меняет viewed/discount session state | High / High | query discount writes session; viewed list writes on render | preserve explicit compatibility, remove query price authority, repeat tests | clear scoped session keys, legacy route | CAT/MONEY |
| R-90 | old/new URLs, slugs, canonical, sitemap/feed regress | High / High | parallel handlers and redirects; numeric/translated identifiers | access logs, redirect map, SEO crawl/schema/feed diff | restore legacy routes/redirect dataset | SEO |
| R-91 | production catalogue/translation edits расходятся после snapshot | High / Critical | live legacy continues writes during migration | one writer, CDC/delta/tombstones, final freeze/hash reconciliation | return read owner to legacy, replay journal; no DB dump rollback | DB/CONTENT |
| R-92 | custom admin routes вне `admin.user` сохранят ACL bypass | High / Critical | 52 declarations после `Voyager::routes()` получают только `web`, guards неоднородны | единый admin auth+explicit ability perimeter, POST/CSRF, actor matrix | route/WAF off, вернуть legacy только после ACL containment | FIL/SEC |
| R-93 | DB-driven BREAD routes/metadata потеряются или разойдутся с route cache | High / High | 133 DataType создают resource/action routes на boot | immutable metadata/route snapshot, static target registry, cached/uncached test | выключить target module, оставить Voyager registry | FIL/APP |
| R-94 | Voyager permissions неверно отобразятся в Filament | High / Critical | 8 ролей, 675 keys, 2 080 assignments; custom actions шире CRUD verbs | deny-by-default policy/action matrix и negative tests | panel/module off, Voyager ACL owner | FIL/SEC |
| R-95 | field/relationship/validation semantics упрощаются | High / High | 1 997 DataRows, 28 relationships, media/rich/custom field types | signed module manifest + field-level parity | вернуть module в Voyager read/write | FIL/DATA |
| R-96 | Voyager и Filament создают lost update переводов/контента | High / Critical | production editors продолжают работать; общий storage | one writer per module+locale, optimistic lock, delta ledger | target writes off, replay journal/legacy owner | FIL/I18N |
| R-97 | media paths/derivatives/delete semantics теряются | High / Critical | MIME `*`, root `/`, move/delete/rename, custom content types | artifact manifest/hash, allowlist, receipt/rollback test | restore exact paths/files, legacy media owner | FIL/FILE |
| R-98 | menu/settings/translated navigation меняют runtime | Medium / Critical | 3 menus/166 items, 27 settings, 767 menu translations | typed mapping, URL/order/locale snapshots, owner UAT | legacy read adapter/table | FIL/CONTENT |
| R-99 | BREAD builder/Database/Compass/media остаются опасными prod tools | Medium / Critical | Compass in production, schema/media operations behind broad panel entry | не переносить schema tools; least privilege; migrations only | disable routes/panel, restore config | FIL/SEC |
| R-100 | stale definitions и неполные controller methods ломают routes | High / High | IDs 27/129 invalid; orders misses standard resource methods | repair/retire decision, reachability/access logs, explicit Pages | disable unresolved route/module | FIL/APP |
| R-101 | 40 custom views/actions/side effects исчезнут при generic CRUD | High / Critical | orders/users/gallery/blog/mail/SEO/ALT overrides | screen/action inventory, specialized Pages, browser/UAT | route back to Voyager screen | FIL/BIZ |
| R-102 | bulk/delete/reorder/action повторится при retry/concurrency | Medium / High | action/relation/reorder routes and side-effect controllers | idempotency receipt, transaction/lock, concurrency tests | stop action, reconcile IDs/artifacts | FIL/DATA |
| R-103 | большие tables/list screens деградируют | High / High | users 23 801, orders 14 423, order_action 6 696; mixed server-side flags | production query profile/index/pagination/bounded selects | disable target resource, legacy screen | FIL/PERF |
| R-104 | production admin edits после snapshot потеряются | High / Critical | catalogue/translations/menu/settings continue changing | CDC/delta/tombstones, component gate, final hashes | return owner legacy, replay forward journal | FIL/DEP |
| R-105 | rollback перезапишет новые target writes | Medium / Critical | нет общего mutation ledger/reverse ownership | additive schema, one writer, forward reconciliation, receipt ledger | disable target writer; no full DB dump restore | FIL/RBK |
| R-106 | PHP language editor повреждает/раскрывает tracked locale files | Medium / Critical | dynamic `include/scandir/fopen` по request-derived locale/template | allowlist, atomic replace, syntax/diff/audit or retire editor | restore versioned dictionary, route off | I18N/SEC |
| R-107 | `ee/et/uk`, file/DB metadata и orphan translations ошибочно объединяются или удаляются | High / Critical | production ALT использует public `ee`, а dirty production tree одновременно содержит отдельные `resources/lang/ee` и `resources/lang/et`; 16 live-modified language files across 8 codes | preserve both file identifiers and all DB rows; explicit locale/source/public-route mapping; seven-public-code golden tests plus `et` file compatibility | restore exact file+DB translation snapshot/delta journal; legacy locale owner | I18N/DATA |
| R-108 | raw `header()+exit` завершает route boot по substring | Medium / Critical | `redirect.php` выполняется при регистрации routes | exact controller redirects, HTTP/console near-match tests | вернуть предыдущую route artifact/map | SEO/APP |
| R-109 | duplicate/conflicting/chained redirects дают недетерминированный target | High / Critical | 7 duplicate groups, 4 conflicts, 3 chains | versioned unique map, one-hop/cycle validator | previous signed map | SEO |
| R-110 | `/pl/*` catch-all теряет path/query и shadow generated redirects | High / High | `/pl/canvas` → homepage `viar-art.pl` | owner-signed cross-domain path/query map, GET/HEAD only | вернуть legacy mapping/domain rule | SEO |
| R-111 | public image debug раскрывает paths и делает outbound probes | High / Critical | debug JSON + `get_headers/file_get_contents` URL | remove public debug, local-first resolver, SSRF allowlist | close routes/WAF, return static fallback | SEC/FILE |
| R-112 | 2 003 Closure routes блокируют route cache | High / High | Laravel 6 serializer throws for Closure | controllers + cached/uncached registry parity | run uncached previous artifact | APP/DEP |
| R-113 | missing `ImageController` блокирует route registry | High / Critical | 11 missing rows, `route:list` fails on image handler | restore/remove after traffic/reference audit | close route, legacy asset fallback | APP |
| R-114 | anonymous translation/meta GET массово пишет catalogue translations/SEO | High / Critical | `/translate_item`, `/set_meta`; TLS verify off; query precedence gap | immediate containment, command dry-run/run ledger/backup | close route, restore row snapshot/journal | SEC/I18N |
| R-115 | public mail preview меняет recovery/coupon и использует private IDs | High / Critical | hardcoded data and DB-mutating preview paths | production remove; synthetic authenticated non-prod preview | close routes, reconcile token/coupon logs | MAIL/SEC |
| R-116 | review upload не валидирует type/size/ownership | High / Critical | raw base64/files; email→user/order lookup | signed order token, FormRequest, quarantine/rate/moderation | disable submission, preserve artifacts for review | SEC/FILE |
| R-117 | gift-card route signature/auth/storage несовместимы | High / Critical | 2 route args vs 3 required IDs; ANY; public write | typed authorized POST/service, immutable artifact policy | close writer, keep prior artifacts | FILE/APP |
| R-118 | robots self-loop и locale/domain sitemap drift | High / High | app `/robots.txt` 302 на себя; 7 locale index/PL conflict | one environment-aware owner, canonical golden tests | static known-good robots/XML cache | SEO/DEP |
| R-119 | feed price state leak/large uncached output | High / Critical | stale `$price_saved`; 7 125 offers; 4.9–11.1 MB | deterministic projection, golden money, cache/stream | restore prior feed owner/cache | FIN/SEO |
| R-120 | enabled Synvolve test receiver сохраняет/раскрывает payload | High / Critical | production `SYNVOLVE_TEST_RECEIVER_ENABLED=true`; unauth capture/latest сохраняют и возвращают полный payload | немедленный production flag-off; затем удалить routes либо auth/IP/redaction/retention + deploy assertion | disable flag/routes, проверить и удалить сохранённые payload/log по политике, просмотреть access evidence | CRM/SEC |
| R-121 | Google Places failure вызывает API amplification/debug leakage | Medium / High | no negative cache/backoff; debug provider error | rate limit, stale/negative cache, no prod debug | fallback-only config | INT/SEO |
| R-122 | duplicate names/route precedence меняют generated URLs/handler | High / High | 5 duplicate-name groups; mixed PL reachability | unique names, executable route matrix/cache parity | previous route artifact | APP/SEO |
| R-123 | live production SEO/catalogue/translation delta теряется при cutover | High / Critical | legacy продолжает принимать writes | watermarks, final delta, one writer, component cutover | return legacy owner; no DB dump restore | DB/SEO/DEP |
| R-124 | нет production crawler/merchant/runtime evidence | High / High | L2/local XML only | anonymized logs, Search Console/feed diagnostics, owner UAT | postpone SEO/feed switch | AUD/SEO |
| R-125 | legacy 404 view способен превратить ожидаемый deny/404 в 500 | High / High | undefined `$trackers_body`; подтверждено feature-прогоном | исправить error-view contract, isolated 404/500 smoke tests | blank response для security gates; вернуть предыдущий error artifact | APP/SEC |
| R-126 | опубликованный review text выполняется как raw HTML и создаёт stored XSS | High / Critical | 6 Blade consumers с `{!! $item->text !!}` | plain-text normalization + escaped render + regression tests | снять review publication, очистить/проверить corpus, вернуть escaped template | SEC/APP |
| R-127 | два review pipeline (`reviews`/`our_works`) расходятся по auth, locale, files, moderation и mail | High / High | 2 POST contracts и 2 tables; legacy locale NULL anomaly | signed owner decision, common submission service/policies, migration map | сохранить оба read-only; отключить один writer после final delta | APP/DB/SEC |
| R-128 | production release выполняется без доказанного immutable/atomic contract | High / Critical | direct mutable `public_html`, нет `current/releases/shared`, deploy/rollback/health scripts не найдены; release owner/runbook не подтверждены | clean staged build, immutable versioned artifact, symlink/atomic switch, migrations/cache/worker/health/rollback runbook + dry run | postpone target release; legacy tree remains owner; rollback only to signed artifact, not ad-hoc Git checkout | AUD/DEP |
| R-129 | пять scheduled jobs не имеют production invocation owner | High / Critical | admin/root crontab пусты; `/etc/cron*`, Hestia config и systemd timers не содержат `schedule:run/work` | exactly-one scheduler, shared locks, timezone, execution metrics/alerts и 30-day outcome ledger | не запускать весь backlog вслепую; определить допустимые jobs и вручную reconcile пропуски | DEP/MAIL |
| R-130 | единственный обнаруженный Redis worker не имеет доказанного lifecycle/coverage contract | High / Critical | systemd worker обслуживает `alt-gen,default` с `--tries=2`; job задаёт `$tries=3`; current sizes 0/0; все 11 214 failures за burst 10.07.2026 = `MaxAttemptsExceededException`; unit/primary-error/restart evidence ещё нет | purpose queues, unit manifest, safe underlying-error classification, reconcile 11 297 failed/1 834 pending suggestions, align attempts+release, outbox/idempotency, drain/restart/depth/failed metrics | не выполнять bulk retry; pause producer/worker по runbook, reconcile suggestion/receipt statuses, controlled restart только после проверки in-flight jobs | QUEUE/DEP |
| R-131 | single/custom/append logs и provider/mail/worker payloads создают disk/PII/secret риск | High / Critical | app logs 323 MiB; Laravel monthly/12 but `create 755`; ALT/worker custom logs explicitly not covered; app `storage/logs/mail.log` coverage unresolved (basename matched system rsyslog only); custom redaction/retention absent | exact full-path size+time rotation for every channel, create 640/600 with runtime group, quotas/redaction/correlation/owner/retention; aggregate-only audit before content access | stop noisy channel, rotate/quarantine by runbook, incident review; не удалять active logs без backup/owner | OBS/SEC |
| R-132 | production backup schedule не даёт проверенного artifact/restore evidence, local Hestia archives отсутствуют и binlog выключен | High / Critical | daily Hestia schedule есть, но local list/archive пусты; `log_bin=OFF`; owner готов делать manual off-host/Google Drive copies, однако artifact/restore ещё не проверены | для каждого checkpoint manual DB+files snapshot, off-host copy, timestamp/size/checksum ledger; isolated restore drill with counts/hashes/RPO/RTO; затем решить automation/PITR | no data migration/cutover до recoverability evidence; never stale dump over live writes; не оставлять единственную копию на production disk | DB/DEP/OPS |
| R-133 | PHP/app/DB timezone drift меняет scheduler и timestamps | High / High | Moscow/Riga/Kiev/Etc-GMT-3 local contexts | UTC event contract, one business timezone, tzdata/DST tests | pause time-based jobs; reconcile UTC ledger | APP/DEP |
| R-134 | local OPcache-off и Windows ZTS дают ложный performance baseline | High / High | target-like PHP process/cache settings absent | production-like staging, OPcache/FPM/process/load profile | reject capacity result; keep legacy capacity | PHP/PERF |
| R-135 | session/cache driver and lifetime drift ломает auth/cart/admin и оставляет слишком длинное окно сессии | High / Critical | production session=database, domain `viarcanvas.com`, Secure=true, lifetime=2880 min (48 h); cache=file; config cache отсутствует | owner-approved cookie/TTL/SameSite/locking policy, admin idle timeout, DB session capacity/cleanup и continuity/failover tests | route traffic to legacy; invalidate production sessions только по согласованному incident/cutover plan | AUTH/DEP |
| R-136 | local Junction/public storage и permissions не воспроизводятся на target | High / Critical | `public/storage` Junction; no release creation/smoke step | explicit link/mount/object-store contract, private/public matrix, capacity tests | target file writers off; legacy storage owner | FILE/DEP |
| R-137 | tracked temp dump/backup artifacts попадают в release | Medium / Critical | `.tmp_order_dump.php`, `modal.blade_backup.php` tracked | artifact allowlist, secret/PII scan, owner disposition | remove artifact from release, rotate leaked secrets if any | SEC/DEP |
| R-138 | runtime proxy/PHP/app/DB limits расходятся | High / High | production Nginx body limit 1024 MiB и MariaDB packet 32 MiB; PHP/app/upload effective boundary и large-payload atomicity не согласованы | capture all effective limits, define smallest authoritative application cap, boundary/failpoint tests | reject oversized payload before side effects; preserve legacy path and source artifact | DB/FILE |
| R-139 | HTTPS/HSTS/proxy/redirect contract зависит от внешней config и `.htaccess` | High / Critical | HTTPS redirect commented; hardcoded production/locale redirects; proxy unknown | one TLS/proxy owner, trusted-proxy tests, golden redirect crawl | restore known-good vhost/redirect artifact; postpone DNS switch | DEP/SEO |
| R-140 | production web/mail/DNS/FTP/panel/DB разделяют один host и общий blast radius | Medium / Critical | production evidence: public web/mail/DNS/FTP/Hestia ports; DB/Redis local | hardening inventory, service isolation, patch/incident owners, off-host backup and recovery rehearsal | isolate/disable nonessential service, restore affected component off-host, keep legacy traffic owner | SEC/DEP |
| R-141 | отсутствие swap и ограниченный общий диск приводят к OOM/disk-full при image/PDF/upload/backup/release peaks | High / Critical | 7.6 GiB RAM, no swap; root 75% used/~19 GiB free | capacity/growth profile, quotas, disk/memory alerts, upload temp limits, off-host backups, staged deploy/load tests | stop producers/uploads, free/quarantine logs/temp, scale/restore service; no blind retry | OPS/FILE/DB |
| R-143 | публичный phpMyAdmin на production vhost подвергается внешним login attempts | High / Critical | `/phpmyadmin` доступен; Nginx location не содержит allowlist/auth_basic/limit_req; Fail2ban jail `phpmyadmin-auth` есть | VPN/IP allowlist или separate admin host, MFA/SSO where possible, rate limit + проверенные jail thresholds, patching/access review; remove unused phpPgAdmin include | close route/include, block at firewall/Nginx, rotate affected credentials, review DB/admin audit logs | SEC/DB |
| R-144 | Production FPM и другие services на host не имеют доказанного memory safety margin при отсутствии swap | High / Critical | production pool до 8 children × 512M; host RAM 7.6 GiB, no swap; другие PHP/DB/mail services активны | measured production RSS/concurrency, lower justified limits, cgroup/systemd guardrails, OOM/disk alerts, staged image/PDF/upload/load tests | stop background producers/nonessential services, shed traffic; investigate OOM before restart/retry | OPS/PHP |
| R-146 | production `.env` доступен на чтение всем local users | High / Critical | mode `664`, owner/group `admin`; secrets/provider/DB values находятся в файле | least privilege 600/640 с проверенным runtime owner, secret manager, file-integrity/permission monitor, local-account audit | restrict access, review local/process access, rotate exposed secrets по приоритету и provider receipts | SEC/DEP |
| R-148 | production Synvolve webhook отключает проверку TLS-сертификата и имени host | High / Critical | `SYNVOLVE_WEBHOOK_VERIFY_SSL=false`; Guzzle получает `verify=false`, `CURLOPT_SSL_VERIFYPEER=false`, `CURLOPT_SSL_VERIFYHOST=0` | включить verify после проверки CA/hostname/chain; production deploy assertion запрещает false; contract smoke без payload | приостановить outbound, проверить provider/transport logs и целостность receipts, повторять только идемпотентные события после исправления TLS | CRM/SEC/INT |
| R-149 | production SMTP не имеет явно настроенного transport encryption | Medium / Critical | `MAIL_PORT=25`, `MAIL_ENCRYPTION=null`; SMTP host внешний по имени, фактический маршрут/STARTTLS ещё не доказаны | доказать loopback/private route либо включить TLS/STARTTLS с certificate verification; synthetic delivery test и mail provider contract | приостановить чувствительные письма, сменить transport credentials при подозрении, reconcile delivery logs без раскрытия адресов | MAIL/SEC |
| R-150 | production database sessions создают высокий 48-hour churn, а ALT failure incident хранится без доказанного disposition | High / Critical | sessions=40 359/357.02 MiB: 40 165 внутри TTL 48 h, 194 stale, none >7 d; failed_jobs=11 214/58.59 MiB, все `alt-gen`/max-attempts; suggestions=11 297 failed + 1 834 pending across 7 locales | traffic/bot/session-write correlation, owner-approved storefront/admin TTL, DB capacity/index/growth; underlying error/status reconciliation и signed failed-job retain/archive/delete decision | не считать session rows пользователями; не выполнять bulk retry/delete; при capacity incident ограничить producers и менять retention только после backup/evidence approval | DB/QUEUE/AUTH |
| R-151 | Redis queue не имеет доказанного memory ceiling и crash-durability contract | High / Critical | Redis 7.0.15 DB 0 подтверждён как Laravel queue DB; current `default/alt-gen` sizes 0/0; `maxmemory=0`, `noeviction`, AOF off, RDB snapshots | выбрать memory/systemd limits и persistence contract, queue depth/age alerts, idempotent producer/outbox и restart-loss rehearsal | остановить producers при росте/OOM, не делать blind replay, восстановить только по durable receipts/reconciliation | REDIS/QUEUE/OPS |
| R-152 | systemd может оборвать ALT job раньше Laravel timeout, а failure loop может повториться | High / Critical | timeout 180 s vs stop grace 90 s; в 11-minute window ровно 132 process exits + failed results + scheduled restarts; no start-limit/permission/missing-file/203/EXEC/OOM markers; затем stable | stop grace > job timeout+safety, graceful deploy drain/queue:restart, safe primary-error review, alert on restart loop and reconciliation | pause producers, verify reserved/receipts, stop with approved grace; do not blind restart/retry after forced kill | QUEUE/DEP/OPS |
| R-153 | production не имеет доказанных application metrics/alerts, а journald/logs потребляют общий диск | High / Critical | common app monitoring agents не найдены; journald 2.4 GiB с default/unset limits, app logs 323 MiB, slow log ~340 MiB, root free ~19 GiB; alert test отсутствует | owner-approved metrics/alerts with disk/OOM/HTTP/DB/queue/scheduler/provider thresholds; explicit journald/log quotas and test alert | shed noisy producers, preserve evidence, free space only by retention runbook; no blind log deletion | OBS/OPS |
| R-154 | production runtime не соответствует одному Git commit и не имеет безопасного rollback artifact | High / Critical | R5 exact: language same=309/changed=23/prod-only=0/local-only=2; 3 untracked contact images; no release dirs/scripts; vendor/lock drift | preserve production/local artifacts, per-key merge/disposition, clean immutable build outside prod, signed manifest и atomic rollback rehearsal | freeze manual edits, capture exact tree+files backup, restore signed prior artifact; do not `git reset/checkout/pull` on live tree | DEP/RBK/I18N |
| R-155 | MariaDB production slow log не покрыт effective logrotate и занимает сотни MiB | High / Critical | `/var/lib/mysql/viarcanvas-slow.log` = ~340.5 MiB; exact active rule covers only `mysqld.log|mariadb.log` under datadir and `/var/log/mysql/*.log`; slow path отсутствует в state | add exact slow-log path to tested MariaDB flush rotation, owner-approved retention/compression/permissions, disk alert; aggregate query digest without SQL/PII | preserve metadata/evidence, rotate only through MariaDB-safe runbook, never truncate/delete active log blindly | DB/OBS/OPS |
| R-156 | delta по `updated_at` пропускает legacy writes | High / Critical | raw `DB::table()->update()` для orders/chat read flags не всегда меняет timestamps; writers распределены между public/admin/account/payment/SA | shared DB first; full PK/canonical-hash reconciliation; mutation receipts; `updated_at` только hint | target writes off, вернуть capability legacy и выполнить full forward reconciliation | DB/DEP/RBK |
| R-157 | физическое удаление заказа не оставляет tombstone и теряется при отдельной DB/snapshot delta | High / Critical | `Orders::deleteOrder()` удаляет `orders`; связанные rows удаляются раздельно; complete delete ledger отсутствует | запрет hard delete во время waves либо immutable tombstone/mutation ledger; PK-set diff; approved retention | остановить delete capability, сохранить orphan evidence, восстановить ownership без старого общего dump | DB/DATA/RBK |
| R-158 | основной client chat не имеет durable unique message key и может дублировать DB/mail/webhook/files | High / Critical | `order_user_comments` local schema не содержит unique external ID; inserts и side effects выполняются рядом; legacy chat tables без FK/order indexes | client_message_id unique, transaction+outbox, source stream/actor mapping, attachment receipts, duplicate/concurrency tests | выключить target chat writer, reconcile source IDs/artifacts/outbox; legacy остаётся owner | CHAT/CRM/FILE |
| R-159 | payment transition распределён между mutable status и неодинаковыми handlers, поэтому rollback/replay повторяет деньги-связанные side effects | High / Critical | Paysera/PayPal/manual/payment-request paths; manual replay способен повторить bonus/coupon/gift PDF/mail; provider attempt/receipt ledger неполон | immutable provider/manual receipt, exact amount/currency/reference, one automatic authority, outbox and provider reconciliation | stop payment writer; classify unknown outcomes; return legacy only after receipt reconciliation; no blind callback replay | PAY/FIN/RBK |
| R-160 | ownership только на уровне таблицы создаёт split-brain между public checkout, admin, SA, callbacks и Filament | High / Critical | одна `orders` изменяется разными каналами и capability; public production продолжает работать во время admin migration | signed capability+field/action matrix, route gates, optimistic lock, one writer per capability, observed cutover | target capability off, drain/reconcile committed writes, re-enable exact legacy capability | FIL/DEP/BIZ |
| R-161 | четыре слоя переводов расходятся или перезаписывают друг друга | High / Critical | R14 final: DB и PHP header delta=`header.top_sale` × 8 locale; 7 mail equal; account local-only `orders.not_specified` × 8; единственный common account value mismatch=`ru/orders.user_status.print_text`; `cn/jp google_reviews` local-only | подписать 27-row owner disposition; единый manifest/versioned artifact; lint/UAT/rollback rehearsal | остановить publisher, сохранить оба DB/files source, активировать предыдущий artifact только после forward reconciliation | I18N/DATA/RBK |
| R-162 | Translation Manager/custom locale editor дают слишком широкий destructive/file-write surface | High / Critical | `admin/translations` статически имеет `web,auth`, delete/publish/remove locale; `AdminLocaleController` делает include/fopen/mkdir; оба пишут `resources/lang` | deny-by-default role/action ACL, path allowlist, audit receipt, один typed versioned publisher, security tests | выключить оба live writers, сохранить filesystem evidence, вернуть только approved legacy action после review | I18N/SEC/FIL |
| R-163 | timestamp-less content/settings/pivots и LTM key без unique constraint теряются в delta | High / Critical | R11 исключил missing/extra identity и status drift на baseline; divergent только 8 values; LTM только PK, duplicates=0 сейчас, hard deletes не видны timestamp scan | locale/group full key/hash/anti-join; tombstone/freeze; повтор duplicate audit; unique DDL только после rehearsal | target writer off, full set reconciliation, quarantine unexplained duplicates/deletes | DB/DATA/I18N |
| R-164 | нормализация locale удаляет или смешивает `ee|et|cn|jp|uk` semantics | High / Critical | R14: header delta=`header.top_sale` во всех 8 locale; account local-only `orders.not_specified` во всех 8 и extra mismatch только RU `orders.user_status.print_text`; `cn/jp google_reviews` local-only и public group consumer подтверждён; `uk->ru` не public locale | signed locale registry/fallback matrix; 27-row disposition; preserve all sources; URL/mail/UI tests | target locale routing/publish off; legacy mapping and previous artifact remain owner | I18N/SEO/CRM |
| R-165 | content/translation cutover ломает slug, canonical, redirects, sitemap/feed и создаёт массовые 404 | High / Critical | URL зависит от base/translated slugs и route/static redirect dataset; отдельной redirects DB-table нет | per-locale URL inventory, collision/chain/loop diff, canonical/hreflang checks, versioned redirect/feed artifacts and crawler smoke | disable target publish/routes, repoint prior artifact, preserve target content edits for forward reconciliation | SEO/DEP/RBK |
| R-166 | catalogue owner split и неполные relations изменяют цены/карточки/SA projection | High / Critical | цены зависят от нескольких tables/options/country/session; известны orphan pivot cohorts; media/translations/SEO связаны | one pricing/content owner, characterization corpus, signed orphan disposition, parent/pivot/media/hash and SA contract reconciliation | target catalogue writer/calculator off; preserve rows/files; legacy returns after price/relation diff | CAT/CRM/DATA |

### P0 containment status — 14.07.2026

R-111/R-114/R-115/R-117/R-120 **не закрыты в коде**. В ходе аудита был проверен временный прототип deny-by-default, но его изменения удалены из приложения после уточнения границ задачи: текущий этап — аудит и клиентский план. Рекомендуемое решение: отдельные flags, production double opt-in, gate до localization redirect и feature tests. До реализации и production ENV/cache/access probes риски считаются открытыми. R-112/R-113/R-116/R-122, SEO/feed R-108–R-110/R-118/R-119 и production evidence R-123/R-124 также открыты.

Review update 14.07.2026: подтверждены ownership, upload/base64, replay/spam и stored-XSS gaps. Временный защитный прототип был удалён; auth-bound ownership, MIME/size/WebM validation, rate limit, cleanup, escaped render, quarantine/AV/retention и production evidence остаются планом работ. R-126 и R-127 открыты.

L3-01 update 16.07.2026: batches 5–8D подтвердили production runtime/DB/Redis/worker/backup schedule, exact dirty release delta, simultaneous `ee/et` file dictionaries, frozen Mix, unrotated ~340 MiB DB slow log and default 2.4 GiB journald. L3-G06 design-аудит добавил R-156…R-166 по ненадёжному delta/delete, chat/payment receipts, capability split-brain, четырём translation layers, destructive dictionary writers, locale/URL и catalogue consistency. Restore, monitoring/alerts, clean artifact, signed owner matrix и cutover/rollback rehearsal всё ещё не доказаны.

## Десять главных

R-02, R-03, R-04, R-05, R-06, R-09, R-23, R-26, R-28, R-29, R-31, R-36, payment gates R-39/R-40/R-41, file gates R-44/R-45/R-46/R-49, delivery/Venipak gates R-50/R-53/R-54/R-55, auth/chat gates R-56/R-57/R-58/R-59/R-60/R-63/R-64, mail/queue gates R-66/R-67/R-68/R-72/R-75, catalogue gates R-78/R-79/R-81/R-82/R-86/R-88/R-91, Voyager/Filament gates R-92/R-94/R-96/R-97/R-101/R-104/R-107, SEO/auxiliary gates R-108/R-109/R-111/R-114/R-115/R-116/R-117/R-118/R-119/R-120/R-123/R-124/R-125/R-126/R-127 и production readiness/cutover gates R-128…R-141/R-143/R-144/R-146/R-148…R-166. Их остаточный риск должен быть формально принят перед production cutover.
