Цель: вынести польскую версию на отдельный домен, настроив глобальный 301 с `viarcanvas.com/pl/*` на `https://viar-art.pl/` без роста 404.

Контекст и размещение
- Мультиязычный проект (старый домен): `C:\OpenServer\domains\asoft\viar\` — рабочий `.htaccess` лежит в `public/.htaccess`.
- Отдельная польская версия: `C:\OpenServer\domains\asoft\viar-art.pl\` (туда уходим редиректом).
- В `public/.htaccess` уже добавлено глобальное правило `^pl/.*$ -> https://viar-art.pl/`; все точечные `/pl/...` редиректы удалены, чтобы не мешать глобальному 301.
- `routes/redirect.php` содержит php-редиректы через `multiLangRedirect`; `.htaccess` срабатывает раньше, поэтому дополнительные изменения не требуются. При будущей чистке можно упростить pl-ветки, но сейчас не критично.
- Правило ведения файла: после каждого действия обновлять чекбоксы/заметки здесь, чтобы сохранять историю выполнения.

Подзадачи (статус)
- [x] Добавить правило ведения журнала: после каждого действия обновлять чекбоксы/заметки в этом файле.
- [x] Проверить `routes/redirect.php` на необходимость правок под задачу (результат: не нужно, `.htaccess` отрабатывает раньше).
- [x] Добавить PHP-фолбэк редирект в `routes/redirect.php` на `/pl/* -> https://viar-art.pl/` (на случай, если .htaccess не срабатывает на окружении).
- [x] Определить активный `.htaccess` для домена `viarcanvas.com` (в проекте используется `public/.htaccess`).
- [x] Добавить правило 301 перед другими редиректами, чтобы любые запросы в `/pl/` уходили сразу на `https://viar-art.pl/`.
- [x] Проверить конфликты с существующими `/pl`-редиректами и убедиться, что новое правило стоит раньше.
- [ ] Прогнать ручной тест: открыть любую старую страницу, например `/pl/graphic-portrait/portrait-beauty-art`, и убедиться, что ответ 301 ведет на `https://viar-art.pl/`.
